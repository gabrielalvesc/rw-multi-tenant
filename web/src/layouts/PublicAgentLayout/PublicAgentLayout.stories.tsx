import type { Meta, StoryObj } from '@storybook/react'

import PublicAgentLayout from './PublicAgentLayout'

const meta: Meta<typeof PublicAgentLayout> = {
  component: PublicAgentLayout,
}

export default meta

type Story = StoryObj<typeof PublicAgentLayout>

export const Primary: Story = {}
